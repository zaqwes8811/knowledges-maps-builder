{
  App: {
    Spider: {
      targets_folder: "./app-spider-targets/",
      intermedia_storage: "./app-folder/intermedia_storage/",
      
      # Доступные ключи
      available_keys: ["to_text"],
      
      # Доступные конверторы
      to_text_convertors: [],
      
      # Могут автоматически детектироваться
      auto_detected_urls: ["srt", "txt"]
    }
  }
}