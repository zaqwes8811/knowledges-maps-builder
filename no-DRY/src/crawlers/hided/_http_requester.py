#-*- coding: utf-8 -*-''' '''import httplibdef get_coursera_stanford_info_page():    """     strs_page = '/algo-003/lecture/index'   # HTTP Error 302 - Moved temporarily """    err = 0    kHeadSite = "class.coursera.org"    kHeadSite = "d19vezwu8eufl6.cloudfront.net"    strs_page = '/algo-003/lecture/index'   # HTTP Error 302 - Moved temporarily    strs_page = '/algo1/slides%2Falgo-asymptotic1.pdf'   # HTTP Error 302 - Moved temporarily        conn = httplib.HTTPConnection(kHeadSite)        data = do_request(kHeadSite, strs_page)    return data    def do_request(domain, path):    err = 0    kHeadSite = domain    conn = httplib.HTTPConnection(kHeadSite)        # Запросы    strs_page = path    conn.request("GET", strs_page)        # Ответ    response = conn.getresponse()    print response.status, response.reason        # Читаем    data = response.read()    conn.close()    return data        return data    if __name__ == '__main__':    print len(get_coursera_stanford_info_page())